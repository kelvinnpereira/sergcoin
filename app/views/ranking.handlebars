<div id="ranking">

	<div class="row">
		<div class="col">
			<h2>Ranking</h2>
			<table class="table table-hover">
				<thead>
					<tr>
						<th scope="col">#</th>
						<th scope="col">Nome</th>
						<th scope="col">Vítorias</th>
					</tr>
				</thead>
				<tbody>
					{{#each users}}
					<tr>
						<td>{{inc @index}}</td>
						<td>{{dataValues.nome}}</td>
						<td>{{dataValues.vitorias}}</td>
					</tr>
					{{/each}}
				</tbody>
			</table>

		</div>
	</div>

</div>