<div id="update" class="container">
    <div class="row">
        <div class="col-sm">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/">Home</a></li>
                    <li class="breadcrumb-item"><a href="/curso">Cursos</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Atualizar Curso</li>
                </ol>
            </nav>
        </div>
    </div>
    <div class="row">
        <div class="col-sm">
            <h2>Atualizar Curso</h2> <br>
            <p>Use o formulario abaixo para atualizar o curso</p> <br> <br>
        </div>
    </div>
    <div class="row">
        <div class="col-sm">
            <form action="" method="POST">
                <div class="form-group">
                    <label for="sigla">Sigla</label>
                    <input type="text" class="form-control" id="sigla" name="sigla" placeholder="IE17"
                        value="{{curso.sigla}}">
                </div>
                <div class="form-group">
                    <label for="nome">Nome</label>
                    <input type="text" class="form-control" id="nome" name="nome" placeholder="Nome"
                        value="{{curso.nome}}">
                </div>
                <div class="form-group">
                    <label for="descricao">Descrição</label>
                    <textarea class="form-control" id="descricao" name="descricao"
                        placeholder="Descrição">{{curso.descricao}}</textarea>
                </div>
                <fieldset class="form-group mt-3">
                    <legend>Área Do Curso</legend>
                    {{#each areas}}
                    <div class="form-check">
                        <label class="form-check-label">
                            <input type="radio" class="form-check-input" name="area" id="{{inc @index}}" value="{{inc @index}}"
                                {{checked ../curso.id_area (inc @index)}}>
                            {{nome}}
                        </label>
                    </div>
                    {{/each}}
                </fieldset>
                <input type="hidden" name="_csrf" value="{{csrfToken}}">
                <input type="submit" class="btn btn-primary" value="Atualizar">
            </form>
        </div>
    </div>

</div>